pyyaml>=6.0              # carga de escenarios (activos protegidos + arquetipos de amenaza) en YAML
jinja2>=3.1.0             # plantillas del informe HTML
plotly>=5.20.0            # visualizacion tactica (radar/PPI) de trazas y anillos de proximidad
fastapi>=0.110.0          # panel web (lanzar un escenario y ver el ranking de amenazas)
uvicorn>=0.29.0           # servidor ASGI para el panel web
python-multipart>=0.0.9   # subida de ficheros de escenario en el panel web
slowapi>=0.1.9            # limite de peticiones por IP en el panel web
httpx>=0.27.0             # cliente HTTP usado por TestClient en los tests de la web (dependencia de desarrollo)
markupsafe>=2.1.0         # comparar el HTML escapado por jinja2 en los tests del informe (dependencia de desarrollo)
pytest>=8.0.0             # framework de tests (dependencia de desarrollo)
pytest-cov>=5.0.0         # cobertura de tests (dependencia de desarrollo)
ruff>=0.16.0              # lint + formato de codigo (dependencia de desarrollo)
mypy>=1.10.0              # comprobacion estatica de tipos (dependencia de desarrollo)
types-PyYAML>=6.0         # stubs de tipado para pyyaml (dependencia de desarrollo)
